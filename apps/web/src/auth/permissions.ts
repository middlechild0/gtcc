// Frontend PermissionKey type, sourced from the shared @visyx/db definition.
// This keeps the web app in lockstep with the backend permissions list.
import type { PermissionKey as DbPermissionKey } from "@visyx/db/permissions";

export type PermissionKey = DbPermissionKey;
